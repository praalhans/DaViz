package com.aexiz.daviz.simulation.algorithm.wave.echo;

import com.aexiz.daviz.simulation.viewpoint.Channel;
import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedState;

public class EchoReceived extends ReceivedState {
    public EchoReceived(Channel channel) {
        super(channel);
    }
}
