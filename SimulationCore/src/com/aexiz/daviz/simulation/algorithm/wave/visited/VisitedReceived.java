package com.aexiz.daviz.simulation.algorithm.wave.visited;

import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedState;
import com.aexiz.daviz.simulation.viewpoint.Channel;

public class VisitedReceived extends ReceivedState {
    public VisitedReceived(Channel channel) {
        super(channel);
    }
}
