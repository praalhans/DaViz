package com.aexiz.daviz.simulation.algorithm.wave.tarry;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.RepliedInformation;

public class TarryReplied extends RepliedInformation {
    public TarryReplied(Channel channel) {
        super(channel);
    }
}