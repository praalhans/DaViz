package com.aexiz.daviz.simulation.algorithm.wave.echo;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.RepliedInformation;

public class EchoReplied extends RepliedInformation {
    public EchoReplied(Channel channel) {
        super(channel);
    }
}