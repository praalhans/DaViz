package com.aexiz.daviz.simulation.algorithm.wave.awerbuch;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedSeemState;

public class AwerbuchReceivedSeem extends ReceivedSeemState {
    public AwerbuchReceivedSeem(Channel channel, boolean seem) {
        super(channel, seem);
    }
}
