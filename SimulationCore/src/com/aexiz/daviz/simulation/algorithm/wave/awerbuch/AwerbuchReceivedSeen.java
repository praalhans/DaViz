package com.aexiz.daviz.simulation.algorithm.wave.awerbuch;

import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedSeenState;
import com.aexiz.daviz.simulation.viewpoint.Channel;

public class AwerbuchReceivedSeen extends ReceivedSeenState {
    public AwerbuchReceivedSeen(Channel channel, boolean seem) {
        super(channel, seem);
    }
}
