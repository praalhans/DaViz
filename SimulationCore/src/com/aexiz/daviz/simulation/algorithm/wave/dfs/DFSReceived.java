package com.aexiz.daviz.simulation.algorithm.wave.dfs;

import com.aexiz.daviz.simulation.viewpoint.Channel;
import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedState;

public class DFSReceived extends ReceivedState {
    public DFSReceived(Channel channel) {
        super(channel);
    }
}
