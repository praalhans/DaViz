package com.aexiz.daviz.simulation.algorithm.wave.cidon;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.RepliedInformation;

public class CidonReplied extends RepliedInformation {
    public CidonReplied(Channel channel) {
        super(channel);
    }
}