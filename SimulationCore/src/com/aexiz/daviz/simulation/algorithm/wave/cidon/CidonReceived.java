package com.aexiz.daviz.simulation.algorithm.wave.cidon;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.state.ReceivedState;

public class CidonReceived extends ReceivedState {
    public CidonReceived(Channel channel) {
        super(channel);
    }
}
