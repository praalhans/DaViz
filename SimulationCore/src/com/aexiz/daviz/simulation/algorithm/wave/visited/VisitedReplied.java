package com.aexiz.daviz.simulation.algorithm.wave.visited;

import com.aexiz.daviz.simulation.Channel;
import com.aexiz.daviz.simulation.algorithm.information.RepliedInformation;

public class VisitedReplied extends RepliedInformation {
    public VisitedReplied(Channel channel) {
        super(channel);
    }
}